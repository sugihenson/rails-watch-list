<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%# <%= f.association :movies %> 
  <%# <%= f.association :bookmarks %> 
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>